diff --git a/hw/i386/xen/xen_platform.c b/hw/i386/xen/xen_platform.c
index 624151eeb810..4e50f593ffb8 100644
--- a/hw/i386/xen/xen_platform.c
+++ b/hw/i386/xen/xen_platform.c
@@ -73,6 +73,7 @@ struct PCIXenPlatformState {
 
     uint16_t class_id;
     uint16_t device_id;
+    uint8_t revision;
 };
 
 #define TYPE_XEN_PLATFORM "xen-platform"
@@ -569,6 +570,7 @@ static void xen_platform_realize(PCIDevice *dev, Error **errp)
 
     pci_set_word(pci_conf + PCI_DEVICE_ID, d->device_id);
     pci_set_word(pci_conf + PCI_CLASS_DEVICE, d->class_id);
+    pci_set_byte(pci_conf + PCI_REVISION_ID, d->revision);
 
     platform_ioport_bar_setup(d);
     pci_register_bar(dev, 0, PCI_BASE_ADDRESS_SPACE_IO, &d->bar);
@@ -592,7 +594,8 @@ static Property xen_platform_props[] = {
     DEFINE_PROP_UINT16("device-id", PCIXenPlatformState, device_id,
                        PCI_DEVICE_ID_XEN_PLATFORM),
     DEFINE_PROP_UINT16("class-id", PCIXenPlatformState, class_id,
-                       PCI_CLASS_OTHERS << 8 | 0x80)
+                       PCI_CLASS_OTHERS << 8 | 0x80),
+    DEFINE_PROP_UINT8("revision", PCIXenPlatformState, revision, 1)
 };
 
 static void xen_platform_class_init(ObjectClass *klass, const void *data)
@@ -604,7 +607,6 @@ static void xen_platform_class_init(ObjectClass *klass, const void *data)
     k->vendor_id = PCI_VENDOR_ID_XEN;
     k->subsystem_vendor_id = PCI_VENDOR_ID_XEN;
     k->subsystem_id = PCI_DEVICE_ID_XEN_PLATFORM;
-    k->revision = 1;
     set_bit(DEVICE_CATEGORY_MISC, dc->categories);
     dc->desc = "XEN platform pci device";
     device_class_set_legacy_reset(dc, platform_reset);
